# Server Configuration
NODE_ENV=development         # Set to 'production' for production deployment
PORT=5000                    # Port number for the backend server

# MongoDB Connection
# Use your local MongoDB URI or MongoDB Atlas connection string
MONGO_URI=mongodb://localhost:27017/reunion-website

# JWT Authentication
# Generate a strong random string for security
JWT_SECRET=your_jwt_secret_at_least_32_characters_long

# SMTP Email Configuration
# Required for sending registration confirmations, payment notifications, etc.
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587              # Common ports: 25, 465 (SSL), 587 (TLS)
EMAIL_SECURE=false          # Set to true for SSL (port 465)
EMAIL_USER=reunion@example.com
EMAIL_PASS=your_email_password
EMAIL_FROM=reunion@classof2005.com

# Reunion Event Settings
REUNION_DATE=2025-09-24
REUNION_LOCATION="Grand Central Hotel, Downtown"
EARLY_BIRD_DEADLINE=2025-06-01
EARLY_BIRD_PRICE=85
REGULAR_PRICE=120
GUEST_PRICE=95

# To use this file:
# 1. Copy this file to .env
# 2. Replace placeholder values with your actual configuration
# 3. Keep your .env file secure and never commit it to version control